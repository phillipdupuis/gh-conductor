.DS_Store

# Claude Code-created worktrees
.claude/worktrees/

# local-only, never commit
docs/

# Local-only files, industry-standard .local convention (CLAUDE.local.md, settings.local.json, .env.local)
*.local
*.local.*

# personal local-only skills/notes (pdupuis-*)
pdupuis-*

# Dependencies (bun)
node_modules/
